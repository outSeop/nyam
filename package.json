{
  "name": "study-fine-manager",
  "version": "1.0.0",
  "description": "스터디 벌금 관리 시스템",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "rm -rf build && mkdir build && cp -r public/* build/ && cp server.js build/ && cp package.json build/ && cp -r models build/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.6.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
